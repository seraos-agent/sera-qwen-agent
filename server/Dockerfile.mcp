FROM node:20-slim

WORKDIR /app

# Install mongodb-mcp-server globally
RUN npm install -g mongodb-mcp-server@latest

# Expose default HTTP port
EXPOSE 8080

# Start server over HTTP transport
CMD ["sh", "-c", "mongodb-mcp-server --transport http --httpHost 0.0.0.0 --httpPort $PORT"]
